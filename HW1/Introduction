The main focus of this homework is clustering. We started out with proving several min/max optimization formula then get hand on by building a k-mean clustering from scratch and use it for image compression. 
